# Core NLP
spacy>=3.5.0
dateparser>=1.1.8

# Retrieval / Scoring — three-tier fallback
sentence-transformers>=2.2.0   # Tier 1: semantic embeddings (all-MiniLM-L6-v2, ~90 MB)
scikit-learn>=1.3.0            # Tier 2: TF-IDF fallback (also used by sbert cosi